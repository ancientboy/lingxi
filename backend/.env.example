# 灵犀云后端配置

# ============ 基础配置 ============

# 服务端口
PORT=3000

# 服务器外网地址（用于生成回调URL）
SERVER_URL=http://your-server-ip:3000

# ============ OpenClaw 配置 ============
# 用于飞书/企业微信消息转发到 OpenClaw

# OpenClaw 服务地址
OPENCLAW_URL=http://localhost:18789

# OpenClaw 访问令牌
OPENCLAW_TOKEN=your-openclaw-token

# OpenClaw 会话 ID
OPENCLAW_SESSION=c308f1f0

# ============ 实例管理 ============

# 实例存储目录
INSTANCES_DIR=/data/lingxi-instances

# OpenClaw 镜像
OPENCLAW_IMAGE=openclaw/openclaw:latest

# 基础端口（每个实例占用一个端口）
BASE_PORT=19000

# MVP 模式（复用现有 OpenClaw 实例）
MVP_MODE=true

# ============ 认证配置 ============

# JWT 密钥
JWT_SECRET=lingxi-cloud-secret-key-2026

# 管理员密钥
ADMIN_KEY=lingxi-admin-2026

# ============ 数据库（MVP 阶段暂不需要）============

# DATABASE_URL=postgresql://user:pass@localhost:5432/lingxi_cloud
# REDIS_URL=redis://localhost:6379

# ============ 方案B: SaaS 远程管理配置 ============

# lingxi 平台地址（用于生成回调URL）
LINGXI_PLATFORM_URL=https://lingxi.cloud

# 部署脚本路径
DEPLOY_SCRIPT_PATH=/home/admin/.openclaw/workspace/lingxi-cloud/scripts/deploy-to-user-server.sh

# 用户服务器统一密码
USER_SERVER_PASSWORD=Lingxi@2026!

# 阿里云 ACR 镜像地址
ACR_REGISTRY=crpi-bcyqkynua4upy5gp.cn-hangzhou.personal.cr.aliyuncs.com/lingxi-cloud2026/lingxi-cloud:latest

# ============ 阿里云 ECS 配置 ============

# 阿里云 AccessKey
ALIYUN_ACCESS_KEY_ID=your-access-key-id
ALIYUN_ACCESS_KEY_SECRET=your-access-key-secret

# 阿里云地域
ALIYUN_REGION=cn-hangzhou

# 安全组 ID（需开放 22, 18789 端口）
ALIYUN_SECURITY_GROUP_ID=sg-xxxxxxxxxx

# 交换机 ID
ALIYUN_VSWITCH_ID=vsw-xxxxxxxxxx

# 镜像 ID（Ubuntu 22.04）
ALIYUN_IMAGE_ID=ubuntu_22_04_x64_20G_alibase_20240819.vhd
