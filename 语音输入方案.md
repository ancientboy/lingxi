# 灵犀云语音输入功能实现方案

## 📊 方案对比

| 方案 | 优点 | 缺点 | 费用 |
|------|------|------|------|
| **Web Speech API** | 浏览器原生、实时识别、免费 | 依赖浏览器支持 | 免费 ✅ |
| 阿里云语音识别 | 准确度高、支持方言 | 需上传音频、延迟高 | ¥0.01/次 |
| 讯飞语音 | 中文识别优秀 | 需要SDK、费用高 | ¥0.015/次 |

**推荐方案：Web Speech API**（免费、实时、浏览器原生）

---

## 🎯 实现方案

### 1. UI 设计

**在输入框左侧添加麦克风按钮：**

```
┌─────────────────────────────────────────┐
│ [🎤] [输入消息...              ] [发送] │
└─────────────────────────────────────────┘

录音状态：
┌─────────────────────────────────────────┐
│ [🔴] [正在录音：你好世界...      ] [发送] │
└─────────────────────────────────────────┘
```

### 2. 技术实现

#### 前端（浏览器端）

```javascript
// 1. 检查浏览器支持
if ('webkitSpeechRecognition' in window || 'SpeechRecognition' in window) {
  // 支持语音识别
  const SpeechRecognition = window.SpeechRecognition || window.webkitSpeechRecognition;
  const recognition = new SpeechRecognition();

  // 配置
  recognition.continuous = true;      // 持续识别
  recognition.interimResults = true;  // 实时显示结果
  recognition.lang = 'zh-CN';         // 中文识别

  // 开始识别
  recognition.start();

  // 实时结果
  recognition.onresult = (event) => {
    const transcript = Array.from(event.results)
      .map(result => result[0].transcript)
      .join('');

    document.getElementById('inputField').value = transcript;
  };

  // 错误处理
  recognition.onerror = (event) => {
    console.error('语音识别错误:', event.error);
  };
}
```

#### 后端（可选备选方案）

如果浏览器不支持，可以提供备选方案：
- 上传音频文件 → 阿里云语音识别 API → 返回文本

---

## 📝 实现步骤

### 第一步：修改 HTML

在 `chat.html` 的输入框区域添加麦克风按钮：

```html
<div class="input-area">
  <div class="input-wrapper">
    <!-- 新增：麦克风按钮 -->
    <button class="voice-btn" id="voiceBtn" onclick="toggleVoiceInput()">
      <i data-lucide="mic" class="icon-sm"></i>
    </button>

    <textarea class="input-field" id="inputField" placeholder="输入消息..." rows="1"></textarea>

    <button class="send-btn" id="sendBtn" onclick="handleSendClick()">
      <i data-lucide="send" class="icon-sm"></i>
    </button>
  </div>
</div>
```

### 第二步：添加 CSS 样式

在 `chat.css` 中添加：

```css
/* 麦克风按钮 */
.voice-btn {
  width: 36px;
  height: 36px;
  border: none;
  background: transparent;
  color: var(--text-secondary);
  cursor: pointer;
  border-radius: 8px;
  transition: all 0.2s;
  display: flex;
  align-items: center;
  justify-content: center;
}

.voice-btn:hover {
  background: var(--bg-tertiary);
  color: var(--text-primary);
}

.voice-btn.recording {
  color: #ef4444;
  background: rgba(239, 68, 68, 0.1);
  animation: pulse 1.5s infinite;
}

@keyframes pulse {
  0%, 100% { transform: scale(1); }
  50% { transform: scale(1.1); }
}

/* 调整输入框宽度 */
.input-field {
  padding-left: 12px;
  margin-left: 8px;
}
```

### 第三步：添加 JS 逻辑

在 `chat.js` 中添加：

```javascript
// 语音识别实例
let recognition = null;
let isRecording = false;

// 初始化语音识别
function initVoiceRecognition() {
  const SpeechRecognition = window.SpeechRecognition || window.webkitSpeechRecognition;

  if (!SpeechRecognition) {
    console.log('浏览器不支持语音识别');
    document.getElementById('voiceBtn').style.display = 'none';
    return;
  }

  recognition = new SpeechRecognition();
  recognition.continuous = true;
  recognition.interimResults = true;
  recognition.lang = 'zh-CN';

  recognition.onresult = (event) => {
    const transcript = Array.from(event.results)
      .map(result => result[0].transcript)
      .join('');

    document.getElementById('inputField').value = transcript;
  };

  recognition.onerror = (event) => {
    console.error('语音识别错误:', event.error);
    stopRecording();
  };

  recognition.onend = () => {
    if (isRecording) {
      // 自动重启（持续录音模式）
      recognition.start();
    }
  };
}

// 切换录音状态
function toggleVoiceInput() {
  if (!recognition) {
    alert('您的浏览器不支持语音识别，请使用 Chrome 或 Edge 浏览器');
    return;
  }

  if (isRecording) {
    stopRecording();
  } else {
    startRecording();
  }
}

// 开始录音
function startRecording() {
  isRecording = true;
  recognition.start();
  document.getElementById('voiceBtn').classList.add('recording');
  document.getElementById('inputField').placeholder = '正在录音...';
}

// 停止录音
function stopRecording() {
  isRecording = false;
  recognition.stop();
  document.getElementById('voiceBtn').classList.remove('recording');
  document.getElementById('inputField').placeholder = '输入消息...';
}

// 页面加载时初始化
document.addEventListener('DOMContentLoaded', initVoiceRecognition);
```

---

## ✅ 功能特性

1. **实时语音识别**
   - 说话时实时转换为文字
   - 边说边显示，无需等待

2. **中文支持**
   - 支持普通话识别
   - 识别准确率高

3. **用户友好**
   - 点击开始/停止录音
   - 录音状态可视化（红色脉冲动画）
   - 识别结果可编辑

4. **浏览器兼容**
   - Chrome ✅
   - Edge ✅
   - Safari ✅
   - Firefox ❌（不支持 Web Speech API）

---

## 🚀 扩展功能（可选）

### 1. 语音命令

```javascript
// 识别特殊命令
if (transcript.includes('发送')) {
  handleSendClick();
}
```

### 2. 多语言支持

```javascript
// 让用户选择语言
recognition.lang = 'en-US';  // 英语
recognition.lang = 'ja-JP';  // 日语
```

### 3. 历史记录

```javascript
// 保存语音识别历史
const voiceHistory = JSON.parse(localStorage.getItem('voiceHistory') || '[]');
voiceHistory.push({
  text: transcript,
  time: new Date().toISOString()
});
localStorage.setItem('voiceHistory', JSON.stringify(voiceHistory));
```

---

## 📱 移动端优化

移动浏览器也支持 Web Speech API：

```javascript
// 检测移动设备
if (/Android|iPhone|iPad|iPod/i.test(navigator.userAgent)) {
  // 移动端：点击后一直录音，直到用户再次点击
  recognition.continuous = true;
}
```

---

## 🔒 隐私说明

- 语音数据在浏览器本地处理
- 不上传到服务器
- 符合隐私保护要求

---

## 📊 预估工作量

| 任务 | 时间 |
|------|------|
| HTML 修改 | 10 分钟 |
| CSS 样式 | 15 分钟 |
| JS 逻辑 | 30 分钟 |
| 测试调试 | 20 分钟 |
| **总计** | **1-1.5 小时** |

---

## 💡 建议

1. **优先实现 Web Speech API**（免费、快速）
2. 如果用户反馈准确度不够，再考虑接入付费 API
3. 可以在设置中添加"语音输入"开关

---

**是否开始实现？** 🎤
