# 🦞 MemOS 官方插件配置指南

## ✅ 已安装

官方 MemOS Cloud OpenClaw Plugin 已成功安装！

**插件类型：** Lifecycle Plugin（生命周期插件）  
**功能：** 自动记忆管理，无需手动调用

## 🎯 工作原理

### 自动记忆召回（对话前）
```javascript
before_agent_start → 从 MemOS 检索相关记忆 → 注入到对话上下文
```

### 自动记忆保存（对话后）
```javascript
agent_end → 保存对话内容到 MemOS
```

## 🔑 配置步骤

### 1️⃣ 获取 API Key

访问：https://memos-dashboard.openmem.net/cn/apikeys/

**免费额度：**
- ✅ 10,000 条记忆
- ✅ 1,000 次对话
- ✅ 100 MB 知识库

### 2️⃣ 配置环境变量

**方式 1：环境变量文件（推荐）**

```bash
# 编辑 ~/.openclaw/.env
echo 'MEMOS_API_KEY=mpg-your-key-here' >> ~/.openclaw/.env
```

**方式 2：Shell 配置**

```bash
# 添加到 ~/.bashrc 或 ~/.zshrc
echo 'export MEMOS_API_KEY="mpg-..."' >> ~/.bashrc
source ~/.bashrc
```

**方式 3：OpenClaw 配置**

```bash
openclaw config set plugins.entries.memos-cloud-openclaw-plugin.config.apiKey "mpg-..."
```

### 3️⃣ 重启 OpenClaw

```bash
openclaw gateway restart
```

### 4️⃣ 验证

重启后查看日志：

```bash
openclaw gateway logs | grep memos
```

应该看到类似输出：
```
[memos-cloud] Plugin loaded
```

## 📖 使用示例

**对话 1：**
```
你：我喜欢简洁直接的回答
我：好的，记住了！
（MemOS 自动保存这条偏好）
```

**对话 2（新会话）：**
```
你：我喜欢什么样的回答？
我：（MemOS 自动召回记忆）你喜欢简洁直接的回答！
```

## 🎛️ 高级配置

编辑 `~/.openclaw/openclaw.json`：

```json
{
  "plugins": {
    "entries": {
      "memos-cloud-openclaw-plugin": {
        "enabled": true,
        "config": {
          "userId": "liege",
          "recallEnabled": true,
          "addEnabled": true,
          "memoryLimitNumber": 6,
          "includePreference": true,
          "includeToolMemory": false,
          "queryPrefix": "important user context "
        }
      }
    }
  }
}
```

### 配置选项

| 选项 | 说明 | 默认值 |
|------|------|--------|
| `userId` | 用户 ID（隔离记忆） | `openclaw-user` |
| `recallEnabled` | 启用记忆召回 | `true` |
| `addEnabled` | 启用记忆保存 | `true` |
| `memoryLimitNumber` | 召回记忆数量 | `6` |
| `includePreference` | 包含用户偏好 | `true` |
| `includeToolMemory` | 包含工具记忆 | `false` |
| `queryPrefix` | 搜索前缀（提高准确率） | `""` |
| `maxQueryChars` | 最大查询字符数 | 无限制 |
| `asyncMode` | 异步模式 | `true` |

## 🚀 性能优势

- ✅ **+43.70% 准确率** vs OpenAI Memory
- ✅ **-35% Token 消耗**（智能检索，不加载完整历史）
- ✅ **毫秒级延迟**（异步 MemScheduler）
- ✅ **自动记忆管理**（无需手动调用）

## 🔄 vs Supermemory

| 特性 | MemOS 官方插件 | Supermemory |
|------|---------------|-------------|
| **自动化** | ✅ 全自动 | ❌ 手动调用 |
| **准确率** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| **Token 节省** | ✅ 35% | ❌ |
| **知识库** | ✅ | ❌ |
| **多模态** | ✅ | ❌ |
| **配置复杂度** | 简单 | 简单 |

## 🛠️ 故障排查

### API Key 无效

```
错误: [memos-cloud] Missing MEMOS_API_KEY
```

**解决：**
1. 确认已配置环境变量
2. 重启 OpenClaw
3. 检查 `~/.openclaw/.env` 文件

### 记忆未召回

**可能原因：**
1. `recallEnabled` 设置为 `false`
2. 搜索结果为空（首次使用）
3. 网络连接问题

**调试：**
```bash
# 查看详细日志
openclaw gateway logs | grep memos
```

## 📚 资源

- 📖 [MemOS 文档](https://memos-docs.openmem.net)
- 🔑 [获取 API Key](https://memos-dashboard.openmem.net/cn/apikeys/)
- 💬 [Discord 社区](https://discord.gg/Txbx3gebZR)
- 🐛 [问题反馈](https://github.com/MemTensor/MemOS/issues)

## 📝 注意事项

1. **API Key 安全** - 不要提交到 Git
2. **用户 ID** - 使用不同的 `userId` 隔离记忆
3. **免费额度** - 超出后需升级付费

---

配置完成后，你的恶魔巴巴塔就有超强记忆了！🦞✨
