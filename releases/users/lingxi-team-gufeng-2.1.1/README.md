# ⚡ 灵犀 AI 团队部署包

你的专属 AI 团队，包含 8 位专业成员。

## 🚀 快速部署

### 方式一：远程部署（从本地推送到服务器）

```bash
./deploy.sh 你的服务器IP
```

### 方式二：本地部署（在服务器上直接运行）

```bash
# 1. 上传整个包到服务器
scp lingxi-team-gufeng-2.1.1.tar.gz root@服务器IP:/root/

# 2. 在服务器上解压并部署
cd /root
tar -xzf lingxi-team-gufeng-2.1.1.tar.gz
cd lingxi-team-gufeng-2.1.1
./deploy-local.sh
```

## 🔗 连接信息

- Session: `676e777e`
- Token: `2e86ba9e2b7cdde2139c601421cd7630`
- 访问地址: `http://你的IP:18789/676e777e?token=2e86ba9e2b7cdde2139c601421cd7630`

## 📱 接入灵犀云

在 [灵犀云](http://120.26.137.51:3000) 中添加服务器即可使用。

## 📦 包含内容

- ✅ 8 位 AI 成员（灵犀、云溪、若曦、紫萱、梓萱、晓琳、音韵、智家）
- ✅ Agent 记忆文件 (SOUL.md)
- ✅ 技能包 (Skills)
- ✅ 模型配置（智谱 GLM-5、通义千问）
- ✅ Gateway 配置（跨域、Token 认证）

## ❓ 常见问题

### 防火墙放行
```bash
sudo ufw allow 18789
```

### 查看日志
```bash
tail -f /var/log/openclaw.log
```

### 重启服务
```bash
pkill -f openclaw
cd ~/.openclaw && nohup openclaw gateway > /var/log/openclaw.log 2>&1 &
```
